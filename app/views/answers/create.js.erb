<% if @answer.errors.present? %>
  <% @answer.errors.full_messages.each do |msg| %>
    $('.answer_errors').html("<%= j msg %>");
  <% end %>
<% else %>
  $('.answers').append('<%= j render 'questions/answers', answer: @answer %>');
  $('#answer_body').val('');
<% end %>
